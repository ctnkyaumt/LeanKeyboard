android.useAndroidX=true
android.enableJetifier=true

# The whisper native library is built for api 21 while the app keeps supporting api 14.
# WhisperLib refuses to load it below api 21, so older systems never reach the native code
# and fall back to the other voice backends.
android.ndk.suppressMinSdkVersionError=21
